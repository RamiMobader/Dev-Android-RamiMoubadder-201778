package com.example.currencyexchangeapp_new.ui.theme

import androidx.appcompat.app.AppCompatDelegate

// This function applies the theme to your app
fun applyTheme() {
    // Set the app theme to light mode (you can change this to dark mode or any custom theme you want)
    AppCompatDelegate.setDefaultNightMode(AppCompatDelegate.MODE_NIGHT_NO)
}
